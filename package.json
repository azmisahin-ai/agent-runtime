{
  "name": "agent-runtime",
  "version": "0.1.0-dev",
  "private": true,
  "description": "Model-agnostic coding Agent Runtime / Harness",
  "type": "module",
  "engines": { "node": ">=22.5.0" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/tests/**/*.test.js",
    "check": "npm run build && npm test",
    "start": "node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  }
}
